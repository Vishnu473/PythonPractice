# 5. s = {}
# What is the type of 's'?


s = {}
print(type(s))
# The type of s is dictionary here. Even though the set is shown using {}, it doesn't mean it's a set. 
# The s type hre is dictionary. As the empty dictionary is declared as {}.

# To create a empty set. Then, you should use s = set()
